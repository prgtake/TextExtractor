# CHANGELOG

すべての変更はこのファイルに記録されます。

## [1.1.1] - 2026-05-20
### 追加
- **カラーログ表示**: 実行ログに重要度別の色分け（成功：緑、エラー：赤、警告：オレンジ、情報：青等）を導入し、視認性を大幅に向上。
- **出力の自動整形機能**: AIが抽出結果をJSON配列（['A', 'B']）で返さず、常に読点区切りの1つの文字列として出力するようシステムプロンプトを強化。これにより、CSV出力時の視認性と利便性が向上。
- **実行前バリデーションの強化**:
    - **SQLiteパス実在確認**: プロンプト内に指定されたSQLiteファイル（.db）がローカルに実在するかを実行前に自動チェック。
    - **RAGストア選択チェック**: RAG利用が有効な際、ストアが未選択の場合に警告を出すガード処理を追加。
- **ファイルフィルタリングの最適化**: 従来の拡張子除外に加え、現在選択中の「指示ファイル（プロンプト）」のみをデータ解析対象から自動で除外するロジックを実装。これにより、データとしての .txt ファイルを漏らさず解析可能に。

### 変更
- **APIエラーハンドリングの精密化**: HTTPステータスコード（429, 503, 500, 504等）や「Deadline Exceeded」等のエラーを個別に判別し、より適切な待機時間で再試行するよう堅牢性を向上。
- **ログメッセージの標準化**: すべての実行ログに重要度レベルを付与し、動作状況の把握を容易に。

## [1.1.0] - 2026-05-10
### 追加
- **一括モード**: 複数ファイル（例：年度別の成績表）を横断して解析し、1つのレコードに統合する機能を追加。
- **SQLiteツール**: プロンプト内で外部SQLiteデータベースを指定し、Geminiが直接データを参照（検索）できる機能を実装（セキュリティのため読み取り専用）。
- **サブフォルダ検索**: 対象フォルダ内のサブフォルダも再帰的に処理できるオプションを追加。
- **WEB検索 / RAG連携**: UIからGoogle検索およびFile Search Store (RAG) を簡単に有効化できるチェックボックスを追加。
- **実行ログ表示**: 処理の進捗をリアルタイムで確認できるログエリアをUI下部に追加。
- **処理の停止ボタン**: 実行中に解析を安全に中断できる「処理を停止」ボタンをUIに追加。
- **AI指示文相談機能**: AI（Gemini）とチャット形式で相談しながら、最適な指示文（プロンプト）を作成できる専用ウィンドウを追加。参考プログラムに倣い、ソースコード読み込みによる高精度なアドバイスと「指示文作成アドバイザー」としての適切な振る舞いを実装。

### 変更
- **API呼び出しの多段階化**: WEB検索、RAG、SQLiteツールなどの「種類の異なるツール」を同時に利用できるよう、調査と合成を分けたステップ実行（マルチステップ）方式に刷新。
- **UIレイアウトの最適化**: ウィンドウの初期高さを調整（850px → 560px）し、ノートPC等の小画面でも扱いやすいサイズに変更。
- **内部設計の刷新**: コードを `TextExtractor` クラスとして再構成し、拡張性と保守性を向上。
- **デフォルトモデルの更新**: 使用モデルの初期値を `gemini-2.5-flash-lite` に設定。
- **設定管理の強化**: APIキーやモデル設定を `app_config.json` で一元管理するようリファクタリング。
- **ブランディングの統一**: 「PRO版」の表記を廃止し、アプリ全体で `v1.1.0` への表記に統一。ヘルプメッセージやウィンドウタイトルを修正。

### 修正
- **API競合の解消**: ツール利用時にJSON出力モード（response_mime_type）が競合してエラーになる問題を、動的な設定切り替えにより修正。
- **堅牢性の向上**: APIがテキストを含まないレスポンスを返した場合にプログラムがクラッシュする問題 (`NoneType` object has no attribute 'strip') を修正。
- **SQLiteセキュリティ**: AIによるDBの破壊を防ぐため、SQLiteツールを `SELECT` 文のみ実行可能な読み取り専用に制限。
- **マージコンフリクトの解消**: 破損していたソースコードおよびドキュメント内のGit衝突マーカーを完全に除去し、正常な動作状態に復元。

## [1.0.0] - 2026-03-20 (推定)
### 追加
- **TextExtractor1 初期リリース**: Google Gemini APIを活用したマルチモーダルデータ抽出ツール。
- **多様なファイル形式への対応**:
    - 画像・PDF (.png, .jpg, .pdf等)
    - Office文書 (.docx, .xlsx, .pptx, .doc, .ppt)
    - 一太郎 (.jtd)
    - メール (.eml, .msg)
    - テキスト・CSV (.txt, .csv, .log)
- **ハイブリッド抽出**: プロンプトによる「【抽出】（事実）」と「【生成】（推論/検索）」の使い分け。
- **データベース保存**: 抽出結果をSQLiteデータベース (.db) に自動保存し、重複処理を回避するスキップ機能を搭載。
- **CSVエクスポート**: 解析結果をExcel等で利用可能なCSV形式（UTF-8 with BOM）で出力。
- **堅牢なエラー処理**: APIのレート制限（429）や過負荷（503）に対し、指数バックオフによる最大5回の自動再試行を実装。
